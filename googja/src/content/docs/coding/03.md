---
title: "03. Spec Kitでアプリの画面骨格とルーティングを生成する"
description: "設計書であるSPEC.mdとDESIGN.mdをもとに、Spec Kitを使って主要な画面構造を自動生成します。"
---

実機での初期起動が確認できたら、いよいよアプリの中身を作っていきます。

最初に行うべきは、細かなボタンや色をいじることではなく、**「アプリ全体の画面骨格（スケルトン）とルーティング（画面遷移構造）」を一気に組み上げること**です。

リサーチ編で作成した `SPEC.md`（要件定義）と、リデザイン編で作成した `DESIGN.md`（画面遷移図）をAIエージェントに渡すことで、Expo Routerに最適化された画面骨格をわずか数分で生成させることができます。

この記事では、Expo Routerの仕組みを理解した上で、AIエージェントに設計書からルーティングを抽出させ、迷いなく動作する画面遷移構造を構築する手順を解説します。

---

## この記事のサブコンテンツ

1. [Expo Routerにおける画面構成の基本ルール（TabsとStack）](#1-expo-routerにおける画面構成の基本ルールtabsとstack)
2. [Spec Kitに設計書からルーティングを抽出させる](#2-spec-kitに設計書からルーティングを抽出させる)
3. [画面骨格を一括生成させるプロンプトテンプレート](#3-画面骨格を一括生成させるプロンプトテンプレート)
4. [生成されたコードの確認（appディレクトリ構造と実装例）](#4-生成されたコードの確認appディレクトリ構造と実装例)
5. [動的ルーティングとモーダル画面の追加設計](#5-動的ルーティングとモーダル画面の追加設計)
6. [実機でのルーティング動作テストチェックリスト](#6-実機でのルーティング動作テストチェックリスト)
7. [まとめと次のステップ](#7-まとめと次のステップ)

---

## 1. Expo Routerにおける画面構成の基本ルール（TabsとStack）

Expo Routerでは、フォルダ構造とファイル名がそのままアプリの画面遷移に対応します。

一般的なモバイルアプリでは、画面下部に常駐する **タブナビゲーション（Tabs）** と、詳細画面や設定画面へ重なるように遷移する **スタックナビゲーション（Stack）** を組み合わせて設計します。

```text
app/
├── _layout.tsx         # 全体のルートレイアウト（Stack）
├── (tabs)/             # 下部タブバーで切り替えるグループ
│   ├── _layout.tsx     # タブバーのアイコン・ラベル・色設定
│   ├── index.tsx       # タブ1: 今日の習慣（ダッシュボード）
│   ├── history.tsx     # タブ2: 達成カレンダー（履歴）
│   └── settings.tsx    # タブ3: 設定
└── modal.tsx           # モーダル表示する画面（下からスライドイン）
```

- **`_layout.tsx`**: そのフォルダ配下の画面共通の枠組み（ヘッダーやタブバー、認証チェックなど）を定義するファイル。
- **`()` 括弧付きのフォルダ**: 「グループ化」を意味し、URLやルートパスには影響を与えずにレイアウトを適用できる。

このルールをAIエージェントに正しく守らせることが、綺麗なルーティングを作る秘訣です。

---

## 2. Spec Kitに設計書からルーティングを抽出させる

AIエージェントにコードを生成させる前に、`DESIGN.md` の「画面遷移とルーティング設計」セクションを意識させます。

習慣管理アプリ（HabitFlow）の場合、MVPで必要な画面は以下の通りです。

1. **ホーム（今日）**: 今日の習慣一覧と1タップ記録（メイン画面）
2. **履歴・統計**: 過去の達成記録や連続日数の振り返り画面
3. **設定**: アプリ情報、通知設定、データ初期化など

AIに「ゼロから考えて」と頼むと不要なログイン画面や複雑なソーシャル機能を勝手に作ってしまうため、**「`SPEC.md` と `DESIGN.md` に記載された画面だけを作れ」** と厳密に制約をかけます。

---

## 3. 画面骨格を一括生成させるプロンプトテンプレート

Antigravity IDE、Claude Code、Cursorなどのチャット入力欄に、以下の指示文をそのまま投入します。

```text
【タスク: アプリの画面骨格とExpo Router設定の生成】

プロジェクトルートにある `SPEC.md` と `DESIGN.md` を読み込んでください。
これらに定義された画面構成に従い、Expo Router用の画面骨格コードを生成してください。

### 要件
1. `app/(tabs)/_layout.tsx` を作成/修正し、DESIGN.mdに定義されたタブ（「今日」「履歴」「設定」）を設定してください。
   - タブアイコンには `@expo/vector-icons`（IoniconsまたはFeather）を使用してください。
   - タブバーのアクティブ色・非アクティブ色は DESIGN.md のカラートークン（Primary: #2563EB, Inactive: #94A3B8）に準拠してください。
2. 以下の各画面ファイルを作成し、仮のタイトルとプレースホルダーテキストを表示するシンプルなコンポーネントを配置してください。
   - `app/(tabs)/index.tsx`: 今日の習慣（メイン画面）
   - `app/(tabs)/history.tsx`: 履歴・カレンダー画面
   - `app/(tabs)/settings.tsx`: 設定画面
3. 不要なサンプルファイル（既存の `explore.tsx` やサンプルコンポーネント等）があれば削除してください。
4. TypeScriptの型エラーや警告が出ないようにしてください。
```

---

## 4. 生成されたコードの確認（appディレクトリ構造と実装例）

AIエージェントの実行後、生成された `app/(tabs)/_layout.tsx` のコードを確認してみましょう。

```tsx
import { Tabs } from 'expo-router';
import { Ionicons } from '@expo/vector-icons';

export default function TabLayout() {
  return (
    <Tabs
      screenOptions={{
        tabBarActiveTintColor: '#2563EB', // DESIGN.mdのPrimaryカラー
        tabBarInactiveTintColor: '#94A3B8',
        tabBarStyle: {
          backgroundColor: '#FFFFFF',
          borderTopColor: '#E2E8F0',
          height: 60,
          paddingBottom: 8,
          paddingTop: 8,
        },
        headerShown: true,
        headerStyle: { backgroundColor: '#FFFFFF' },
        headerTitleStyle: { fontWeight: '700', color: '#0F172A' },
      }}
    >
      <Tabs.Screen
        name="index"
        options={{
          title: '今日',
          headerTitle: 'HabitFlow',
          tabBarIcon: ({ color, size }) => (
            <Ionicons name="checkbox-outline" size={size} color={color} />
          ),
        }}
      />
      <Tabs.Screen
        name="history"
        options={{
          title: '履歴',
          headerTitle: '達成カレンダー',
          tabBarIcon: ({ color, size }) => (
            <Ionicons name="calendar-outline" size={size} color={color} />
          ),
        }}
      />
      <Tabs.Screen
        name="settings"
        options={{
          title: '設定',
          headerTitle: '設定',
          tabBarIcon: ({ color, size }) => (
            <Ionicons name="settings-outline" size={size} color={color} />
          ),
        }}
      />
    </Tabs>
  );
}
```

各タブ画面（`index.tsx`, `history.tsx`, `settings.tsx`）にも、SafeAreaViewとViewでラップされた基本的なJSXが配置されます。

---

## 5. 動的ルーティングとモーダル画面の追加設計

習慣の編集画面や新規作成画面を「下からニュッとせり上がるモーダル」として表示したい場合、Expo Routerなら `app/_layout.tsx` に1行追加するだけで実現できます。

```tsx
// app/_layout.tsx
import { Stack } from 'expo-router';

export default function RootLayout() {
  return (
    <Stack>
      {/* タブグループはヘッダー非表示で埋め込む */}
      <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
      {/* モーダル画面の定義 */}
      <Stack.Screen
        name="modal"
        options={{
          presentation: 'modal',
          headerTitle: '習慣の追加',
        }}
      />
    </Stack>
  );
}
```

このように、タブとモーダルの階層をAIに指示するだけで、ネイティブアプリ特有の洗練された画面遷移が数行で組み上がります。

---

## 6. 実機でのルーティング動作テストチェックリスト

スマホ実機（Expo Go）を見てみましょう。ホットリロードによって即座に画面が更新されているはずです。

- [ ] 画面下部に「今日」「履歴」「設定」の3つのタブが表示されているか
- [ ] 各タブをタップしたときに、ヘッダータイトルとコンテンツがスムーズに切り替わるか
- [ ] アイコンとアクティブ時のカラー（Primaryカラー: `#2563EB`）が正しく反映されているか
- [ ] iPhoneのノッチ（画面上部の切り欠き）やホームバーと重ならず、セーフエリアが確保されているか
- [ ] ターミナルにルーティング関連の警告（Warning）が出ていないか

アプリの「器（骨格）」ができたことで、次はこの器の中に具体的なUIパーツを流し込んでいきます。

---

## 7. まとめと次のステップ

画面骨格とルーティングを固めたことで、アプリの全体像が実機上で手にとって触れるようになりました。

次の記事では、リデザイン編（Phase03）でGoogle Stitchを使って定義した美しいUIパーツ（カード、進捗バー、ボタンなど）を、AIエージェントに指示してReact NativeのUIコンポーネントとして実装する「[04. Spec KitでStitchのデザインをUIコンポーネントに実装する](../04/)」に進みましょう。
