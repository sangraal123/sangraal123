---
title: "04. ストア決済とサブスクリプションを実装する"
description: "RevenueCatやExpo In-App Purchasesを用い、アプリ内でのデジタルコンテンツ販売や定期購読（サブスク）を実装します。"
---

アプリ開発者が夢見るゴールのひとつが、**「毎月安定して発生するサブスクリプション（定期購読）収益」**です。

しかし、iOSのStoreKitとAndroidのGoogle Play Billingは、仕様、レシート検証、解約・更新通知の処理がそれぞれ異なり、自前でサーバーを立てて連携するのは開発者にとって最大の悪夢でした。

その複雑さをすべて解決し、現在世界のモバイルアプリ開発でデファクトスタンダードとなっているのが **「RevenueCat」** です。

この記事では、RevenueCatを使って、iOS/Android共通のサブスクリプション課金および広告削除の買い切り課金をExpoアプリへ組み込む実践手順を詳しく解説します。

---

## この記事のサブコンテンツ

1. [なぜ自前実装ではなく「RevenueCat」を使うべきなのか](#1-なぜ自前実装ではなくrevenuecatを使うべきなのか)
2. [ストア側での課金アイテム（プロダクト）登録](#2-ストア側での課金アイテムプロダクト登録)
3. [RevenueCatダッシュボードでのEntitlement（権利）設計](#3-revenuecatダッシュボードでのentitlement権利設計)
4. [RevenueCat SDK（react-native-purchases）の導入と初期設定](#4-revenuecat-sdkreact-native-purchasesの導入と初期設定)
5. [ペイウォール（購入画面）の実装と「購入の復元」ボタン](#5-ペイウォール購入画面の実装と購入の復元ボタン)
6. [課金ステータス（Entitlements）の判定とプレミアム機能の制限解除](#6-課金ステータスentitlementsの判定とプレミアム機能の制限解除)
7. [無料トライアルとプロモーションコードの活用戦略](#7-無料トライアルとプロモーションコードの活用戦略)
8. [まとめと次のステップ](#8-まとめと次のステップ)

---

## 1. なぜ自前実装ではなく「RevenueCat」を使うべきなのか

AppleとGoogleの課金システムには、以下のような無数の落とし穴があります。

- レシート（購入証明）の暗号署名検証をサーバーサイドで常に行う必要がある
- クレジットカード決済の失敗、猶予期間（Grace Period）、プラン変更（アップグレード/ダウングレード）の追跡
- ユーザーが別端末に乗り換えた時の「購入の復元」処理

RevenueCatは、これらの複雑なバックエンド処理、Webhook、レシート検証をすべてクラウド側で肩代わりしてくれます。個人開発者であれば**月間売上が約2,500ドル（約38万円）に達するまで完全無料**で利用できます。

---

## 2. ストア側での課金アイテム（プロダクト）登録

まずは各ストア管理画面で課金プロダクトを作成します。

- **プロダクトIDの命名規則**: `com.yourname.app.premium.monthly` のように命名します。
- **タイプ**:
  - **自動更新サブスクリプション**: 例: 「HabitFlow Pro（月額350円 / 年額3,000円）」
  - **非消費型アイテム（Non-Consumable）**: 例: 「広告非表示（買い切り500円）」

---

## 3. RevenueCatダッシュボードでのEntitlement（権利）設計

RevenueCatでは、App StoreのプロダクトとGoogle Playのプロダクトを、1つの抽象的な「権利（Entitlement）」にまとめます。

- **Entitlement Identifier**: `pro`
- **Attached Products**:
  - Apple: `com.yourname.habitflow.pro.monthly`
  - Google: `habitflow_pro_monthly`

これにより、コード上では「このユーザーは `pro` を持っているか？」だけを判定すればよく、OSの違いを意識する必要が一切なくなります。

---

## 4. RevenueCat SDK（react-native-purchases）の導入と初期設定

Expoプロジェクトに RevenueCat のパッケージをインストールします。

```bash
npx expo install react-native-purchases
```

※ RevenueCatはネイティブコードを含むため、Expo Goではなく EAS Build による開発クライアント（Development Build）またはスタンドアロンビルドで動作確認します。

アプリの起動時（`app/_layout.tsx`）で初期化します。

```typescript
import { useEffect } from 'react';
import { Platform } from 'react-native';
import Purchases from 'react-native-purchases';

const APIKeys = {
  apple: process.env.EXPO_PUBLIC_REVENUECAT_APPLE_KEY || '',
  google: process.env.EXPO_PUBLIC_REVENUECAT_GOOGLE_KEY || '',
};

export default function RootLayout() {
  useEffect(() => {
    Purchases.setLogLevel(Purchases.LOG_LEVEL.DEBUG);

    if (Platform.OS === 'ios') {
      Purchases.configure({ apiKey: APIKeys.apple });
    } else if (Platform.OS === 'android') {
      Purchases.configure({ apiKey: APIKeys.google });
    }
  }, []);

  return <Slot />;
}
```

---

## 5. ペイウォール（購入画面）の実装と「購入の復元」ボタン

ユーザーにプレミアムプランを提示するモーダル画面を作成します。審査通過に必須となる「復元ボタン」「利用規約リンク」を忘れずに含めます。

```typescript
// components/PaywallModal.tsx
import React, { useState } from 'react';
import { View, Text, TouchableOpacity, Alert, StyleSheet } from 'react-native';
import Purchases, { PurchasesPackage } from 'react-native-purchases';

export function PaywallModal({ onClose }: { onClose: () => void }) {
  const [loading, setLoading] = useState(false);

  const handlePurchase = async (pkg: PurchasesPackage) => {
    try {
      setLoading(true);
      const { customerInfo } = await Purchases.purchasePackage(pkg);
      if (customerInfo.entitlements.active['pro'] !== undefined) {
        Alert.alert('ありがとうございます！', 'プレミアム機能がアンロックされました。');
        onClose();
      }
    } catch (e: any) {
      if (!e.userCancelled) {
        Alert.alert('エラー', e.message);
      }
    } finally {
      setLoading(false);
    }
  };

  const handleRestore = async () => {
    try {
      const customerInfo = await Purchases.restorePurchases();
      if (customerInfo.entitlements.active['pro'] !== undefined) {
        Alert.alert('復元完了', '過去の購入履歴が復元されました。');
        onClose();
      } else {
        Alert.alert('お知らせ', '有効な購入履歴が見つかりませんでした。');
      }
    } catch (e: any) {
      Alert.alert('エラー', e.message);
    }
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>HabitFlow Pro</Text>
      <Text style={styles.subtitle}>無制限の習慣登録とAIコーチによるアドバイス</Text>

      {/* 審査に必須の「購入の復元」ボタン */}
      <TouchableOpacity onPress={handleRestore} style={styles.restoreBtn}>
        <Text style={styles.restoreText}>以前の購入を復元する（Restore Purchases）</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { padding: 24, alignItems: 'center' },
  title: { fontSize: 24, fontWeight: 'bold', color: '#0F172A' },
  subtitle: { marginTop: 8, color: '#64748B', textAlign: 'center' },
  restoreBtn: { marginTop: 20 },
  restoreText: { color: '#2563EB', textDecorationLine: 'underline', fontSize: 13 },
});
```

---

## 6. 課金ステータス（Entitlements）の判定とプレミアム機能の制限解除

ユーザーが有料会員かどうかを判定するカスタムフック（`useSubscription`）を作ります。

```typescript
// hooks/useSubscription.ts
import { useState, useEffect } from 'react';
import Purchases from 'react-native-purchases';

export function useSubscription() {
  const [isPro, setIsPro] = useState(false);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const checkProStatus = async () => {
      try {
        const customerInfo = await Purchases.getCustomerInfo();
        setIsPro(customerInfo.entitlements.active['pro'] !== undefined);
      } catch (e) {
        setIsPro(false);
      } finally {
        setLoading(false);
      }
    };

    checkProStatus();

    // 課金ステータスの変更（解約・購入・更新）をリアルタイム監視
    Purchases.addCustomerInfoUpdateListener((info) => {
      setIsPro(info.entitlements.active['pro'] !== undefined);
    });
  }, []);

  return { isPro, loading };
}
```

アプリ内の各所で `const { isPro } = useSubscription();` を呼び出し、`!isPro` の場合は「習慣の登録上限を3個まで」に制限したり、広告を表示したりするロジックを組み込みます。

---

## 7. 無料トライアルとプロモーションコードの活用戦略

初期のユーザー獲得には「最初の2週間無料トライアル」が絶大な威力を発揮します。

StoreKitおよびGoogle Play Console側で「無料試用（Free Trial）」を設定しておくだけで、RevenueCatの購入フローが自動的に「トライアル付き購入」として処理してくれます。リスクなく課金体験を試してもらうことで、有料転換率を最大化できます。

---

## 8. まとめと次のステップ

サブスクリプション課金の導入により、あなたのアプリは継続的なビジネスを生み出す本物のプロダクトへと進化しました。

最後の総仕上げとして、今後のアップデートや機能追加でもアプリが壊れないように自動テストを整備し、GitHubにプッシュするだけで自動ビルド・配布が行われるCI/CDパイプラインを構築する「[05. 自動テストとCI/CDを構築して開発を安定させる](../05/)」に進みましょう。
